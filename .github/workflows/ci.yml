name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}

      - name: Cache MongoDB binaries
        uses: actions/cache@v3
        with:
          path: ~/.cache/mongodb-memory-server
          key: ${{ runner.os }}-mongodb-6.0.5

      - name: Install dependencies
        run: npm ci
        env:
          MONGOMS_DOWNLOAD_URL: https://fastdl.mongodb.org/linux/mongodb-linux-x86_64-ubuntu2004-6.0.5.tgz
          MONGOMS_VERSION: 6.0.5

      - name: Lint
        run: npm run lint

      - name: Run tests with coverage
        run: npm run test:ci
        env:
          MONGOMS_VERSION: 6.0.5

      - name: Check coverage files
        run: |
          ls -la coverage/ || echo "Coverage directory not found"
          if [ -f coverage/lcov.info ]; then
            echo "lcov.info found, showing first 10 lines:"
            head -10 coverage/lcov.info
          else
            echo "lcov.info not found"
          fi

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io

      - name: Send coverage to Coveralls
        uses: coverallsapp/github-action@v2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path-to-lcov: ./coverage/lcov.info
